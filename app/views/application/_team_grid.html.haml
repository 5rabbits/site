%ul.team-list
  - team.each do |member|
    %li.member
      = link_to member_path(name: member['name']) do
        = render partial: 'member_image', locals: { member: member }

        .member-name
          = member['name'].split(' ').first
